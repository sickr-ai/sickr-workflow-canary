{
  "name": "@sickr/workflow-canary",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic fixture repo used by SICKR workflow simulation and canary runs.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
